//GET A FUNCTION THAT MAKES THE COMPUTER RETURN RANDOMLY "ROCK", "PAPER" or "SCISSOR"

function getComputerChoice(list){
    return list[Math.floor(Math.random()*list.length)];
}
    const list = (["rock", "paper", "scissor"])

